import "../assets/images"

export const useImageBase = (imageName) => {
    return "../assets/images" + imageName;
}

export const useLottieBase = (lottieName) => {
    return "../assets/lottie" + lottieName;
}